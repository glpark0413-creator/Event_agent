# 모바일 스포츠 게임 이벤트 기획 자동화 에이전트 — 통합 설계서

> **용도**: Claude Code 구현 참조용 계획서
> **작성 기준**: 기존 Google Sheets 참조 → 신규 이벤트 기획안 생성 → 일정/내용 인터랙티브 검토 → Google Sheets 출력

---

## 1. 작업 컨텍스트 문서

### 1-1. 배경 및 목적

모바일 게임(야구 등 스포츠 및 다양한 장르)의 이벤트 기획 담당자가 매월 반복적으로 수행하는 이벤트 기획안 작성 작업을 자동화한다. 기존 Google Sheets의 양식·포맷을 100% 보존하면서, 시즌·마켓 트렌드 및 장르 특화 문구를 반영한 신규 이벤트 기획안을 빠르게 생성하고, 담당자의 검토를 거쳐 최종 Google Sheets로 출력하는 것이 목적이다.

### 1-2. 범위

| 포함 | 제외 |
|------|------|
| 기존 Sheets 구조 분석 및 템플릿 추출 | 이벤트 실제 개발/구현 |
| 장르 선택 및 키워드 추천·확인 (AskUserQuestion) | 게임 내 데이터 연동 |
| 시즌·장르 기반 이벤트 문구/제목 생성 | 다국어 번역 |
| 참조 탭 선택 (AskUserQuestion) | 이미지·배너 생성 |
| 이벤트 수 자동 판단 (월별 평균 기반) | 타 메신저/시스템 연동 |
| 인터랙티브 일정·내용 검토 (Phase 3) | |
| 이벤트 보상 명칭 입력 및 수량 자동 조정 | |
| Google Sheets API 출력 | |

### 1-3. 입출력 정의

**입력**
- 기존 이벤트 Google Sheets URL 또는 xlsx 파일 경로 (읽기 권한)
- 대상 연월 (예: `2025-05`)
- 타겟 마켓 (예: 일본, 한국, 글로벌)
- 게임 장르 (AskUserQuestion 2단계 선택 — 계열 → 세부 장르, 16개 장르 지원)

**출력**
- 신규 이벤트 기획안이 담긴 Google Sheets URL 또는 xlsx 파일 (쓰기 권한 계정으로 생성)
- 중간 산출물: `/output/` 내 JSON 파일들 (단계별 보존)

### 1-4. 제약조건

- 기존 Sheets의 열(Column) 구조, 데이터 포맷, 카테고리 분류를 변경하지 않는다
- Google Sheets API 사용 (OAuth 2.0 인증 또는 서비스 계정)
- AskUserQuestion 도구는 옵션 최대 4개 제한 → 16개 장르를 2단계 선택(계열→세부)으로 처리
- 이벤트 수는 기존 문서의 월별 평균에서 ±1 범위 내로 생성 (명시적 지시 없을 시)

### 1-5. 용어 정의

| 용어 | 정의 |
|------|------|
| 기존 문서 | 담당자가 URL 또는 파일로 제공하는 참조용 Google Sheets / xlsx |
| 양식 템플릿 | 기존 문서에서 추출한 열 구조 + 포맷 정의 |
| 이벤트 초안 | Phase 2에서 생성된 미확정 기획안 (임시 일정 포함) |
| 확정 기획안 | Phase 3 검토 완료 후 최종 승인된 이벤트 데이터 |
| 타겟 시즌 | 대상 연월에 해당하는 시즌 이벤트 맥락 (골든위크, 추석 등) |
| 장르 계열 | AskUserQuestion 1차 선택지 (액션·슈팅 / RPG·전략 / 캐주얼 / 스포츠) |
| 세부 장르 | AskUserQuestion 2차 선택지 (핵앤슬래시, FPS, 야구 등 16종) |
| genre_phrases | 장르 웹서치 및 요청자 확인을 거쳐 확정된 이벤트 키워드 목록 |
| 참조 탭 | 신규 시트 생성 시 복사 기준이 되는 기존 xlsx 탭 |

---

## 2. 워크플로우 정의

### 2-1. 전체 흐름도

```
[시작: 요청자 입력]
    │  - 파일 경로 / Sheets URL
    │  - 대상 연월, 타겟 마켓
    ▼
[0단계] 장르 선택 (AskUserQuestion × 2회)
    │  Q1: 장르 계열 선택 (액션·슈팅 / RPG·전략 / 캐주얼 / 스포츠)
    │  Q2: 세부 장르 선택 (계열별 최대 4개 옵션)
    ▼
[0.5단계] 키워드 추천 및 확인 (웹서치 + AskUserQuestion)
    │  웹서치 2회 → 10개 이상 키워드 수집
    │  Q3: 추천 키워드 사용 / 직접 입력 / 일부 선택
    │  산출물: genre_phrases 확정
    ▼
[1단계] 생성 탭명 확인
    │  생성할 탭 이름(YYMMDD) 입력받기
    ▼
[2단계] 참조 탭 선택 (AskUserQuestion, 탭당 1회)
    │  xlsx 최신 날짜형 탭 최대 3개 옵션 + 직접 입력
    │  복수 탭: 첫 선택 후 "나머지도 동일?" 단축 질문
    ▼
[3단계] 업데이트 내용 확인 (선택)
    ▼
[Phase 1] 기존 문서 분석
    │  스크립트: Sheets API / xlsx로 데이터 읽기
    │  LLM: 열 구조·포맷·카테고리 의미 해석
    │  산출물: template.json, history_stats.json
    ▼
[Phase 2] 이벤트 초안 생성
    │  LLM: 시즌 맥락 판단 + 이벤트 수 결정 + 문구 생성
    │  산출물: draft_events.json
    ▼
[이벤트 날짜·헤더 자동 갱신]
    │  create_tabs.py 실행 → 날짜/이달의선수팩 패턴 치환
    ▼
[이벤트 명칭 자동 갱신]
    │  extract_event_names.py → historical_event_names.json
    │  (0.5단계에서 확정된 genre_phrases 재사용, 웹서치 스킵)
    │  LLM: 섹션 제목 제안 → 요청자 번호 선택 → event_names_config.json 저장
    │  create_tabs.py 재실행
    ▼
[이벤트 보상 수정]
    │  Q: 신규 보상 명칭 직접 입력 / 없음 (AskUserQuestion)
    │  기존 탭 전체 스캔 → 보상 유형별 평균 산출
    │  Q: 평균 수량 적용 / 직접 수정 / 수량 유지 (AskUserQuestion)
    │  xlsx 재저장
    ▼
[Phase 3] 인터랙티브 검토
    │  LLM: 이벤트별 순차 제시
    │  요청자: 일정·내용·보상 수정 또는 유지 입력
    │  산출물: confirmed_events.json
    ▼
[Phase 4] Google Sheets / xlsx 출력
    │  스크립트: Sheets API 또는 xlsx 저장
    │  산출물: 최종 파일 URL / 경로
    ▼
[완료: 결과 반환]
```

### 2-2. 단계별 상세 정의

---

#### 0단계 — 장르 선택 (AskUserQuestion)

| 항목 | 내용 |
|------|------|
| **처리 주체** | AskUserQuestion (요청자 선택) |
| **진입 조건** | 세션 시작 시 장르가 미확정인 경우 |
| **출력** | 확정 장르명 (세부 장르 문자열) |
| **스킵 조건** | 세션 내 장르가 이미 확정된 경우 |

**Q1 — 장르 계열 선택** (옵션 4개):

| 옵션 | 포함 장르 |
|------|------|
| 액션·슈팅 | 핵앤슬래시 / FPS / TPS |
| RPG·전략 | MMORPG / 턴제 / 전략 / RTS / MOBA / AOS |
| 캐주얼 | 시뮬레이션 / 어드벤처 / 퍼즐 / 리듬 / 로그라이크 / 덱빌딩 |
| 스포츠 | 야구 / 축구 / 농구 / 기타 스포츠 |

**Q2 — 세부 장르 선택** (계열별 최대 4개 옵션, Other = 직접 입력):

| 계열 | 옵션 1 | 옵션 2 | 옵션 3 | 옵션 4 |
|------|------|------|------|------|
| 액션·슈팅 | 핵앤슬래시 | FPS | TPS | — |
| RPG·전략 | MMORPG | 턴제 | 전략·RTS | MOBA·AOS |
| 캐주얼 | 시뮬레이션·어드벤처 | 퍼즐 | 리듬 | 로그라이크·덱빌딩 |
| 스포츠 | 야구 | 축구 | 농구 | 기타 스포츠 |

---

#### 0.5단계 — 키워드 추천 및 확인 (AskUserQuestion)

| 항목 | 내용 |
|------|------|
| **처리 주체** | 웹서치(LLM) + AskUserQuestion(요청자) |
| **진입 조건** | 0단계 완료 직후 |
| **출력** | `genre_phrases` 확정 목록 (event_names_config.json에 저장) |

**실행 순서**

1. 웹서치 2회 실행:
   - `"{장르} 모바일 게임 이벤트 문구 키워드"`
   - `"{장르} 게임 이벤트 제목 사례"`
2. 수집 키워드 10개 이상 목록 제시
3. **Q3 — AskUserQuestion** (옵션 3개):
   - `추천 키워드 사용` — 수집 키워드 전체를 genre_phrases로 확정
   - `직접 입력` — 원하는 키워드를 쉼표 구분 입력
   - `추천에서 일부 선택` — 번호 선택 또는 추가 입력

> 확정된 genre_phrases는 이후 이벤트 명칭 자동 갱신 웹서치를 **스킵**하는 조건이 된다.

---

#### 1단계 — 생성 시트 수 및 탭명 확인

생성할 탭명(YYMMDD 형식)이 요청에 명시되지 않은 경우 텍스트로 입력받는다.

---

#### 2단계 — 참조 탭 선택 (AskUserQuestion)

| 항목 | 내용 |
|------|------|
| **처리 주체** | AskUserQuestion (요청자 선택) |
| **진입 조건** | 0.5단계(장르·키워드 확정) 완료 이후 |
| **출력** | 탭별 참조 탭명 매핑 |

**질문 형식** — 생성 탭 1개당 1회 `AskUserQuestion` 호출:
- 질문: `"{새탭명}" 시트는 어떤 기존 탭을 참조할까요?`
- 옵션 (최대 4개):
  1. `{최근탭1}` — xlsx 날짜형(YYMMDD) 탭 최신순 1위
  2. `{최근탭2}` — 2위
  3. `{최근탭3}` — 3위
  4. `직접 입력` — 위 목록 외 탭 지정

**복수 탭 단축 처리**: 첫 번째 탭 선택 직후 나머지 탭도 동일 참조 탭 적용 여부 질문 (`예, 모두 동일하게` / `아니요, 개별 선택`).

---

#### 3단계 — 업데이트 내용 확인 (선택)

요청에 업데이트 내용(보상 변경, 섹션 추가 등)이 있으면 추출해 적용하고, 없으면 날짜·헤더 갱신만 진행한다.

---

#### Phase 1 — 기존 문서 분석

| 항목 | 내용 |
|------|------|
| **처리 주체** | 스크립트(Sheets API / xlsx 읽기) + LLM(구조 해석) |
| **입력** | 기존 Sheets URL 또는 xlsx 경로 |
| **출력** | `template.json` (열 구조·포맷), `history_stats.json` (월별 이벤트 수 통계) |
| **성공 기준** | 필수 열(이벤트명, 기간, 보상, 상세내용) 추출 완료 + 월별 평균 계산 완료 |
| **실패 시 처리** | 접근 오류 → 에스컬레이션 / 열 구조 파악 불충분 → 자동 재시도 1회 후 에스컬레이션 |

**LLM 판단 영역**
- 열 이름 약어·비표준 명칭 의미 추론 (예: `ev_nm` → 이벤트명)
- 날짜 포맷 다양성 해석
- 카테고리 분류 체계 파악 (출석, 미션, 한정 등)

---

#### Phase 2 — 이벤트 초안 생성

| 항목 | 내용 |
|------|------|
| **처리 주체** | LLM |
| **입력** | `template.json`, `history_stats.json`, 대상 연월, 타겟 마켓, 확정 장르 |
| **출력** | `draft_events.json` (임시 일정 포함 이벤트 목록) |
| **성공 기준** | ① 이벤트 수가 월별 평균 ±1 범위 내 / ② 모든 이벤트가 필수 열을 채움 / ③ 임시 일정이 대상 연월 범위 내 |
| **실패 시 처리** | 규칙 위반 → 자동 재시도 최대 2회 / 품질 기준 미달 → 재생성 후 재검증 |

**이벤트 문구 생성 기준**
```
시즌 맥락 + 장르 특화 + 타겟 마켓
예시:
  - 전반기(시즌) + 홈런(야구) + 글로벌 → "전반기 홈런 챌린지"
  - 얼리썸머(시즌) + 핵앤슬래시 콤보 → "얼리썸머 연속 콤보 미션"
```

---

#### 이벤트 날짜·명칭 자동 갱신

create_tabs.py 실행 후 항상 수행한다.

1. `extract_event_names.py` 실행 → `historical_event_names.json`
2. 웹서치 (0.5단계에서 genre_phrases 확정 시 **스킵**)
3. LLM이 섹션별 신규 명칭 제안 → 요청자 번호 선택 → `event_names_config.json` 저장
4. create_tabs.py 재실행

---

#### 이벤트 패턴 갭 분석 & 누락 이벤트 추천

이벤트 보상 추천·승인 완료 직후 실행한다.
소스 xlsx 전체 탭의 역사적 이벤트 구성 패턴을 학습하고, 신규 탭에서 빠진 이벤트를 식별해 추가를 제안한다.

| 항목 | 내용 |
|------|------|
| **처리 주체** | 스크립트(analyze_event_patterns.py) + LLM(이벤트 생성) + AskUserQuestion(추가 여부) |
| **입력** | 소스 xlsx, output xlsx, 신규 탭명 목록 |
| **출력** | `event_pattern_analysis.json`, (선택) 갭 이벤트가 추가된 output xlsx |
| **성공 기준** | 갭 분석 표 제시 → 사용자 응답 처리 완료 |
| **실패 시 처리** | 스크립트 오류 시 경고만 출력 후 건너뜀 (블로킹하지 않음) |

**스크립트 실행**:
```bash
python scripts/analyze_event_patterns.py \
    "{source_xlsx}" \
    "{output_xlsx}" \
    "{탭명1},{탭명2}"
```

**갭 분석 출력 형식** (탭별 순서대로 제시):
```
[이벤트 패턴 갭 분석] — {장르} / {대상월} / {탭명} 탭
───────────────────────────────────────────────────────────────────
 이벤트 유형         | 역사 등장률          | 이번 탭  | 우선순위
───────────────────────────────────────────────────────────────────
 출석_이벤트         | ████ 100% (21/21)    | ✅ 있음  | —
 미션_이벤트         | ████ 100% (21/21)    | ✅ 있음  | —
 패스                | ████ 100% (21/21)    | ✅ 있음  | —
 던전_이벤트         | ███○  52% (11/21)    | ❌ 없음  | ⚠ 추가 권장
 할인_이벤트         | █○○○  29% ( 6/21)    | ❌ 없음  | 〇 선택 사항
───────────────────────────────────────────────────────────────────
 이번 탭: 5개 | 역사 평균: 5.2개 (범위: 4~7개)
```

**우선순위 분류**:

| 등장률 | 분류 | 처리 |
|---|---|---|
| ≥ 80% | ❗ 누락 확인 필요 | `"건너뜀"` 선택 시에도 경고 출력 |
| 50~80% | ⚠ 추가 권장 | `"추가"` 응답의 기본 대상 |
| 30~50% | 〇 선택 사항 | `"전체 추가"` 응답 시 포함 |
| < 30% | (표시 없음) | 자동 추천 제외 |

**이벤트 자동 생성 규칙** (추가 응답 시):
- 섹션 번호: 현재 탭 마지막 번호 + 1
- 이벤트명: `{세션 키워드} + {이벤트 유형 특성어} + {이벤트 유형}`
- 기간: 탭 전체 이벤트 기간 동일 적용
- 보상: `reward_by_event.json` 해당 유형 역사 평균값

**분기 조건**:
- `"건너뜀"` → 현재 구성 유지, 다음 단계로
- `"추가"` / 번호 선택 / 이름 지정 → 이벤트 생성 후 미리보기 제시 → 사용자 확인 → xlsx 저장
- ❗ 항목 존재 시 → `"건너뜀"` 선택해도 경고 메시지 출력

---

#### 이벤트 보상 수정

이벤트 제목·날짜·명칭 수정 완료 직후 실행한다.

| 단계 | 내용 |
|------|------|
| **보상 명칭 입력** | AskUserQuestion: `보상 명칭 직접 입력` / `없음` |
| **보상 수량 분석** | xlsx 소스의 모든 날짜형 탭 스캔 → 보상 유형별 평균 산출 |
| **수량 적용 확인** | AskUserQuestion: `평균 수량 적용` / `직접 수정` / `수량 유지` |
| **xlsx 재저장** | 보상 수정 내역 반영 후 output 파일 덮어쓰기 |

**보상 수량 분석 출력 형식**:
```
[보상 수량 분석 결과]
─────────────────────────────────────────
보상 유형      | 탭별 수치 범위      | 적용 평균값
다이아         | 30 ~ 100 개        | 50 개
골드           | 5,000 ~ 50,000     | 20,000
픽업팩         | 1 ~ 3 개           | 2 개
─────────────────────────────────────────
```

---

#### Phase 3 — 인터랙티브 검토

| 항목 | 내용 |
|------|------|
| **처리 주체** | LLM(진행·반영) + 요청자(판단) |
| **입력** | `draft_events.json` |
| **출력** | `confirmed_events.json` |
| **성공 기준** | 모든 이벤트에 대해 요청자의 명시적 승인("유지" 또는 수정 완료) 확인 |
| **실패 시 처리** | 응답 모호 → 에이전트 재질문 / 세션 중단 → `confirmed_events.json` 에 진행 상태 저장 후 재개 가능 |

**인터랙션 프로토콜**
```
─────────────────────────────
[N/전체] {이벤트명}
  기간: {시작일} ~ {종료일}
  카테고리: {카테고리}
  보상: {보상 내용}
  상세: {상세 내용}

→ 유지 / 수정할 항목과 내용을 입력해주세요.
─────────────────────────────
```

**분기 조건**
- 이벤트 추가 요청 → 새 항목 생성 후 검토 큐에 추가
- 이벤트 삭제 요청 → 해당 항목 제거 후 다음으로 진행
- 전체 검토 완료 후 최종 확인("전체 확정") → Phase 4 진입

---

#### Phase 4 — Google Sheets / xlsx 출력

| 항목 | 내용 |
|------|------|
| **처리 주체** | 스크립트(Sheets API 또는 xlsx 저장) |
| **입력** | `confirmed_events.json`, `template.json` |
| **출력** | 신규 Google Sheets URL 또는 xlsx 파일 경로 |
| **성공 기준** | ① 신규 시트/파일 생성 완료 / ② 모든 이벤트 행 기록 완료 / ③ 기존 양식 일치 |
| **실패 시 처리** | API 오류 → 자동 재시도 최대 3회 / 권한 오류 → 에스컬레이션 |

---

### 2-3. 상태 전이 요약

```
IDLE
  → [요청자 입력 수신] → GENRE_SELECTING
GENRE_SELECTING (0단계)
  → [장르 확정] → KEYWORD_CONFIRMING
KEYWORD_CONFIRMING (0.5단계)
  → [genre_phrases 확정] → TAB_CONFIGURING
TAB_CONFIGURING (1~3단계)
  → [탭명·참조탭·업데이트 내용 확정] → ANALYZING
ANALYZING (Phase 1)
  → [분석 성공] → DRAFTING
  → [분석 실패·에스컬레이션] → WAITING_USER
DRAFTING (Phase 2)
  → [초안 생성 성공] → REWARD_EDITING
  → [재시도 초과] → WAITING_USER
REWARD_EDITING (보상 수정)
  → [보상 수정 완료] → REVIEWING
REVIEWING (Phase 3)
  → [이벤트별 순차 확인 중] → REVIEWING (루프)
  → [전체 확정] → OUTPUTTING
  → [세션 중단] → PAUSED (재개 가능)
OUTPUTTING (Phase 4)
  → [생성 성공] → DONE
  → [API 실패·에스컬레이션] → WAITING_USER
DONE
```

---

## 3. 구현 스펙

### 3-1. 폴더 구조

```
/event-planner-agent
  ├── CLAUDE.md                          # 메인 에이전트 지침 (오케스트레이터)
  ├── app.py                             # Streamlit 웹 UI (API 키 불필요 모드)
  ├── /scripts
  │   └── create_tabs.py                 # xlsx 탭 생성 스크립트
  ├── /.claude
  │   ├── /skills
  │   │   ├── /sheets-reader             # Google Sheets / xlsx 읽기
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts
  │   │   │       └── read_sheet.py
  │   │   ├── /template-extractor        # 양식 템플릿 추출·저장
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts
  │   │   │       └── extract_template.py
  │   │   ├── /event-generator           # 이벤트 문구·초안 생성
  │   │   │   ├── SKILL.md
  │   │   │   └── /references
  │   │   │       └── season_calendar.md # 시즌·공휴일 참조 데이터
  │   │   ├── /review-conductor          # Phase 3 인터랙션 진행
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts
  │   │   │       └── update_draft.py
  │   │   └── /sheets-writer             # Google Sheets / xlsx 출력
  │   │       ├── SKILL.md
  │   │       └── /scripts
  │   │           └── write_sheet.py
  │   └── /agents
  │       └── (단일 에이전트 구조 — 서브에이전트 없음)
  ├── /Readdocs
  │   └── [FB_GL] 2026 라이브 이벤트.xlsx  # 소스 xlsx (읽기 전용)
  ├── /output
  │   ├── template.json                    # Phase 1 산출물
  │   ├── history_stats.json               # Phase 1 산출물
  │   ├── draft_events.json                # Phase 2 산출물
  │   ├── confirmed_events.json            # Phase 3 산출물
  │   ├── event_names_config.json          # 장르·키워드·이벤트명 치환 설정
  │   ├── historical_event_names.json      # 기존 탭 이벤트명 패턴 추출 결과
  │   ├── event_pattern_analysis.json      # 이벤트 유형별 역사 등장률 + 신규 탭 갭 분석
  │   └── run_log.json                     # 전체 실행 로그
  └── /docs
      └── google_auth_setup.md           # Google API 인증 설정 가이드
```

### 3-2. 에이전트 구조

**단일 에이전트 채택 근거**
- 전체 단계가 순차적 의존 관계(앞 단계 산출물 → 다음 단계 입력)
- 장르·키워드 정보가 0단계에서 확정되어 이후 전 단계에 걸쳐 참조됨
- Phase 3 인터랙션 상태를 하나의 에이전트가 관리하는 것이 자연스러움

→ CLAUDE.md 단일 파일이 전체 오케스트레이션 담당

### 3-3. CLAUDE.md 핵심 섹션 목록

1. **역할 정의** — 이벤트 기획 자동화 에이전트 목적 및 행동 원칙
2. **입력 수집 절차** — 시작 시 받아야 할 정보 및 세션 재개 감지
3. **0단계** — 장르 계열·세부 장르 2단계 AskUserQuestion 선택
4. **0.5단계** — 웹서치 키워드 추천 + AskUserQuestion 확인 + genre_phrases 확정
5. **1~3단계** — 탭명 확인 / 참조 탭 AskUserQuestion 선택 / 업데이트 확인
6. **이벤트 날짜·명칭 자동 갱신** — create_tabs.py + extract_event_names.py 실행 지침
7. **이벤트 보상 수정** — 보상 명칭 입력(AskUserQuestion) + 수량 평균 산출 + 수량 적용 확인(AskUserQuestion)
8. **Phase별 실행 지침** — 진입 조건, 호출 스킬, 산출물 저장 경로
9. **Phase 3 인터랙션 프로토콜** — 제시 형식, 수정 반영 방법, 종료 조건
10. **에스컬레이션 기준** — 언제 요청자에게 확인을 요청하는가
11. **실패·재시도 정책** — Phase별 최대 재시도 횟수

### 3-4. 스킬 목록 및 트리거 조건

| 스킬명 | 역할 | 트리거 조건 |
|--------|------|------------|
| `sheets-reader` | Google Sheets URL / xlsx에서 전체 데이터 읽기 | Phase 1 시작 시 |
| `template-extractor` | 읽은 데이터에서 열 구조·포맷·통계 추출 | Phase 1, sheets-reader 완료 후 |
| `event-generator` | 시즌·장르 기반 이벤트 문구 및 초안 생성 | Phase 2 시작 시 |
| `review-conductor` | Phase 3 인터랙션 진행 및 수정 사항 반영 | Phase 3 시작 시, 요청자 응답마다 |
| `sheets-writer` | 확정 데이터를 신규 Google Sheets / xlsx에 기록 | Phase 4 시작 시 (전체 확정 후) |
| `create_tabs.py` | xlsx 탭 복사 + 날짜·명칭·보상 치환 | 이벤트 시트 직접 생성 요청 시 |

### 3-5. 주요 산출물 파일 형식

**`template.json`** — 열 구조 및 포맷 정의
```json
{
  "columns": [
    { "index": 0, "name": "이벤트명", "type": "string", "required": true },
    { "index": 1, "name": "카테고리", "type": "enum", "values": ["출석", "미션", "한정"], "required": true },
    { "index": 2, "name": "시작일", "type": "date", "format": "YYYY/MM/DD", "required": true },
    { "index": 3, "name": "종료일", "type": "date", "format": "YYYY/MM/DD", "required": true },
    { "index": 4, "name": "보상", "type": "string", "required": true },
    { "index": 5, "name": "상세내용", "type": "string", "required": false }
  ]
}
```

**`history_stats.json`** — 월별 이벤트 수 통계
```json
{
  "monthly_avg": 6,
  "monthly_counts": { "2025-01": 5, "2025-02": 6, "2025-03": 7 },
  "category_ratio": { "출석": 0.3, "미션": 0.5, "한정": 0.2 },
  "reward_range": { "다이아": { "min": 30, "max": 100, "avg": 50 } }
}
```

**`draft_events.json` / `confirmed_events.json`** — 이벤트 목록
```json
{
  "target_month": "2025-05",
  "market": "글로벌",
  "genre": "야구",
  "events": [
    {
      "id": 1,
      "status": "pending",
      "이벤트명": "전반기 홈런더비 챌린지",
      "카테고리": "미션",
      "시작일": "2025/04/29",
      "종료일": "2025/05/05",
      "보상": "다이아몬드 50개",
      "상세내용": "기간 내 홈런 10개 달성 시 보상 지급"
    }
  ],
  "review_progress": { "confirmed": 0, "total": 6 }
}
```

**`event_names_config.json`** — 장르·키워드·이벤트명 치환 설정
```json
{
  "genre": "야구",
  "target_month": "2026-06",
  "genre_phrases": ["전반기", "올스타", "홈런", "만루", "끝내기"],
  "event_name_replacements": {
    "260611": [
      ["얼리썸머 14일 출석 이벤트!", "전반기 14일 출석 이벤트!"]
    ],
    "260618": []
  }
}
```

**`run_log.json`** — 실행 로그 (에스컬레이션·실패 이력 포함)
```json
{
  "run_id": "2025-05-01T10:00:00",
  "phases": [
    { "phase": 1, "status": "success", "timestamp": "..." },
    { "phase": 2, "status": "retried", "attempts": 2, "timestamp": "..." }
  ],
  "escalations": []
}
```

### 3-6. Google API 연동 요건

- **인증 방식**: OAuth 2.0 (개인 계정) 또는 서비스 계정 JSON 키
- **필요 스코프**: `https://www.googleapis.com/auth/spreadsheets`
- **사용 API**: Google Sheets API v4
  - `spreadsheets.values.get` — Phase 1 읽기
  - `spreadsheets.create` + `spreadsheets.values.update` — Phase 4 쓰기
- **인증 설정 참조**: `/docs/google_auth_setup.md`

---

## 4. 설계 결정 사항 및 근거

| 결정 | 선택 | 근거 |
|------|------|------|
| 에이전트 구조 | 단일 에이전트 | Phase 간 컨텍스트 공유 빈번, Phase 3 상태 관리 단순화 |
| 데이터 전달 | 파일 기반 (`/output/*.json`) | Phase 간 데이터 크기 크고, 세션 중단 시 재개 가능성 확보 |
| 이벤트 수 결정 | 월별 평균 ±1 자동 판단 | 요청자 개입 최소화, 기존 패턴 일관성 유지 |
| Phase 3 범위 | 일정+내용+보상 전체 수정 | 요청자 최종 품질 보장, 에이전트 생성물의 오류 수정 창구 확보 |
| Sheets 출력 | API로 신규 시트 생성 | 기존 문서 오염 방지, 요청자의 2차 편집 용이성 |
| 장르 선택 방식 | AskUserQuestion 2단계 (계열→세부) | AskUserQuestion 옵션 최대 4개 제한 내에서 16개 장르 수용 |
| 키워드 확정 방식 | 웹서치 → AskUserQuestion 3지선다 | 자동 추천으로 편의성 확보, 요청자가 최종 결정권 유지 |
| 참조 탭 선택 | AskUserQuestion (최신 3개 + 직접 입력) | 텍스트 입력 오류 방지, 사용 가능 탭을 직접 확인 후 선택 |
| 보상 수량 결정 | 기존 탭 전체 스캔 → 유형별 평균 산출 | 수동 조사 없이 일관된 수량 기준 제공, 요청자 최종 승인 |
| 보상 명칭 갱신 | AskUserQuestion (입력/없음) | 명칭 변경 필요 여부를 요청자가 명시적으로 결정 |
